
export const { filter } = Array.prototype;

export const iterateProperties = function * () {
  for (const key in this) {
    yield [ key, this[key] ];
  }
};

export const compact = function() {
  return this::filter(n => n);
};

export const isString = function() {
  return (typeof this == 'string') || (this instanceof String);
}

export const unique = function() {
  return [ ...new Set(this) ];
};
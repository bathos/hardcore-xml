
import { Writable } from 'stream';

export default class Tokenizer extends Writable {
  constructor(decoder) {
    super({ decodeStrings: false });

    this.decoder = decoder;
  }

  _write(chunk, enc, cb) {

//    if (decoder.ambiguous) {
//      decoder.morePlease();
//    }

  }
}